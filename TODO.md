# TODO - Button visibility issue

- [ ] Update `src/elements/ButtonElement.tsx`:
  - [ ] Add `stroke` + `strokeWidth` to Konva `Rect` to ensure button outline is visible
  - [ ] Center the `Text` vertically inside the button (remove hardcoded `y + 15`)

- [ ] Run project (dev server) and confirm button is visible on canvas.

